#include "main.h"

/**
 * main - Computes the absolute value of an integer
 *
 * @i: Inputs number as an integer
 * reyurn: absolute value
 */

int _abs(int i);
{
	if (i >= 0)
	{
		return (0);
	}
	else
	{
		return (i * -1);
	}
}
