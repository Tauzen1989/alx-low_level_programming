#include <stdio.h>
/**
 * main - Entry point
 * Discription: 'print all single digits of base 10'
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0 ; i <= 9; i++)
	{
		printf("%d", i);
	}
		printf("\n");

	return (0);
}
